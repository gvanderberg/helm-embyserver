apiVersion: v1
kind: Service
metadata:
  name: {{ include "embyserver.fullname" . }}
  labels:
    {{- include "embyserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "embyserver.selectorLabels" . | nindent 4 }}
