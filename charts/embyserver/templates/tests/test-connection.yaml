apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "embyserver.fullname" . }}-test-connection"
  labels:
    {{- include "embyserver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "embyserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
